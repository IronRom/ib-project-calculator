# ИБ Калькулятор ПИР

## Архитектура

> При изменении архитектуры — вносить правки в эту секцию.

**Стек**: Next.js 14 (App Router) + FastAPI + PostgreSQL + Claude API (claude-sonnet-4-6)
**Дизайн**: `design/DESIGN_SYSTEM.md`

### Поток данных

```
Upload ТЗ файлы в проект
  → POST /api/projects/{id}/calculate
  → document_parser: PDF/DOCX → plain text
  → entity_extractor: Claude API → ExtractionResult
  → Экран /entities: таблица для валидации (Категория / Тип / Адрес / X / Коэф.)
  → [Фаза 2] confirm → calculator → results → export 2ПС ИР
```

### Data models (PostgreSQL)

- `reference_books` — СБЦП/МРР версии (requires_validation → consistent → archived)
- `book_object_types` — типы объектов из справочника (КНС, Водовод, ...), извлекаются при парсинге
- `reference_rows` — строки справочника (table_num, x_min, x_max, a, b)
- `price_indices` — квартальные индексы Минстроя (Прил.3 п.1 = проектные работы)
- `users` — role: admin|user, can_calculate=false по умолчанию
- `projects` — контейнер файлов ТЗ per user
- `project_files` — загруженные файлы ТЗ (pdf_path, extracted_text)
- `calculations` — хранит book_version_id (FK, зафиксирован), extracted_entities JSONB
- `audit_logs`

### Классификация объекта (AI извлекает из ТЗ)

- **Категория**: new_construction | reconstruction | overhaul
- **Тип объекта**: из `book_object_types` активного справочника
- **Адрес/регион**: влияет на сейсмику, МРР и другие коэффициенты

### Формула расчёта

```
(a + b×X) × коэф. × индекс × стадия_фактор × (1 + НДС)
```

Экстраполяция (МУ №620 Прил.1 / 707/пр п.131): `a + b × (0.4 × X_гран + 0.6 × X_задан)`.
Ниже Xмин/2 — 707/пр ф.8.4/8.5: цена в точке Xмин/2, умноженная на Кэ = X/(0.5·Xмин), Кэ ≥ 0.1.
Таблицы только с «а» (b=NULL, но есть границы) — 707/пр п.133 ф.8.6–8.8:
интерполяция между опорными точками; вне точек — наклон крайнего сегмента × 0.6.

**Штучные строки** (b=NULL и нет диапазона x_min/x_max): цена за единицу → `a × X`,
где X = количество (ячейки, шкафы, пункты). Характерно для НЗ (СИТО табл. 3.13 и т.п.).

**ПД/РД split** — per book: `reference_books.pd_pct / rd_pct` (NULL → МУ №620 п.1.4: 0.4/0.6).
НЗ задают своё распределение (707/пр п.17-д), например НЗ-847 табл.2.3: ПД=0.6, РД=0.4.
Редактируется в admin (PATCH /admin/references/{id}).

### Admin panel

Кастомная `/admin` секция в Next.js. Не Payload CMS, не react-admin.

Функции:
- Пользователи: toggle can_calculate, роли
- Справочники: загрузить PDF → parse → скачать Excel → загрузить Excel → активировать/откатить
- Каждый справочник имеет `parse_prompt` (кастомизируется в admin)
- Индексы Минстроя: добавить вручную per квартал

### Entity extractor — многопроходная логика

`backend/app/services/entity_extractor.py`

```
Step 0  _build_book_list()         → AI определяет нужные справочники из текста ТЗ
Pass 1  _build_types_context()     → AI извлекает позиции (category/type/address/X/qty)
        _build_hints_context()       вместе с типами: правила из book_extraction_hints
        _fill_sbts_codes()          детерминированный: table_num → book.code из БД
Pass 2  _build_conditions_context() → AI проставляет коэффициенты (keyed conditions)
Pass 3  _build_resolve_x_context() → AI заполняет null x_value (targeted call)
        _merge_resolved_x()          никогда не перезаписывает уже заполненные x_value
        _validate_entities()         sanity check: tz_quote + x_value в тексте ТЗ
```

**Данные для AI только из БД**, не из кода:
- Типы объектов → `book_object_types` (type_id, name, table_num)
- Правила извлечения → `book_extraction_hints` (trigger_condition, hint_for_ai)
- Коэффициенты → `book_conditions` (coeff_key, coeff_min, coeff_max, row_range)

`ANTHROPIC_API_KEY` — в `.env`, не в коде.

> **AI-провайдер: OpenRouter, НЕ Anthropic API** (решение от 22.07.2026 — ключ
> Anthropic пуст и не пополняется). Экстракция ТЗ — `extract_entities_openrouter()`
> (модель через параметр `?model=` у /stream). Ещё НЕ переведены на OpenRouter
> (захардкожен anthropic client, использовать нельзя до перевода):
> `reference_parser.py` (vision-парсер справочников), `document_parser._parse_pdf_vision`
> (OCR сканов), `correct_and_compute` (calculations.py).

### Calculation engine

`backend/app/services/calculator.py`

**Поиск строки** (`_match_row`):
1. Exact range match с unit conversion (UNIT_CONVERSIONS dict)
2. Extrapolation МУ №620 Прил.1: `a + b × (0.4·X_гран + 0.6·X_задан)`
3. Fallback: если object_type_id даёт 0 строк → retry без фильтра по типу

**Коэффициенты**:
- `_resolve_coeff_values()`: заменяет AI-флаг (value=1.0) на реальный coeff_min из book_conditions
  Lookup: table-specific → global (table_num=NULL)
- `_apply_coefficients()` по МУ №620 п.3.14:
  - ценообразующие (reconstruction, overhaul) → перемножить
  - усложняющие (asu, seismic, deepening, fishery) → сумма дробных частей + 1

**Формула** (русский формат): `(147380+242530*0,15316)*1,2`
**Обоснование**: `СБЦП 81-2001-17, табл. 9, п. 1 (до 0,25); п. 2.9 (АСУ К=1,2)`

**Warnings** (result["warnings"], показывать пользователю всегда):
- коэффициент заявлен AI, но условия нет в book_conditions → «НЕ применён, цена занижена»
- индекс пересчёта отсутствует или старше текущего квартала

Все AI-вызовы (экстракция, vision, коррекция) — `temperature=0` (детерминизм смет).

### ПРАВИЛО УНИВЕРСАЛЬНОСТИ — обязательно соблюдать

> Система рассчитана на **любой** СБЦП/МРР из любой отрасли. Их будут десятки.

**НЕЛЬЗЯ хардкодить:**
- коды справочников (81-2001-17, МРР и т.п.) в calc engine или extractor
- номера таблиц или диапазоны X для конкретного типа объекта
- значения коэффициентов (все берутся из `book_conditions`)
- названия типов объектов (все из `book_object_types`)
- любую логику вида "если тип=КНС → таблица 9" или "если ГНБ → x_value = ..."

**МОЖНО (и нужно):**
- добавлять записи в `book_extraction_hints` — правила per справочник, редактируемые в admin
- добавлять записи в `book_conditions` — коэффициенты per справочник/таблица
- добавлять unit conversions в `UNIT_CONVERSIONS` dict (универсальные)
- улучшать общие алгоритмы match/extrapolation/apply

Нарушение этого правила — всегда техдолг. Если хочется хардкодить → ищи способ вынести в БД.

### Проценты разделов ПД/РД (book_section_shares)

Каждая позиция может считаться не на полный состав документации, а на долю
фактически разрабатываемых разделов (как в сметах экспертов: 86%, 94,5%, 18%…).

- `book_section_shares` (миграция a7b8c9d0e1f2): book_version_id, table_num
  (NULL = вся книга), stage ('ПД'|'РД'), section_code, pct. Источник — таблицы
  «относительной стоимости разделов» самих СБЦ/НЗ/МРР
- Entity: `sections` (список кодов из ТЗ, пусто = 100%) или ручной оверрайд
  `pd_sections_pct`/`rd_sections_pct` (дробь 0-1, приоритетнее sections)
- Движок: Σ pct выбранных разделов per стадия; коды сверяются без префикса
  «ИОС.» (_norm_section_code); unmatched коды и отсутствие распределения → warning
- Сиды: `seed_ms53_section_shares.py` (НЗ-53, все таблицы приложения кроме
  row_range-коллизий — см. докстринг), МРР-4.2 в import-скрипте
- Техдолг: несколько распределений на одну таблицу (по пунктам) требуют
  поддержки row_range в lookup — сейчас посеян наиболее употребимый вариант

### Roadmap (не реализуем сейчас)

- `chapter_coefficients` — раздело-специфичные коэф. (пример: ×1.2 к разделу КР за прогрессирующее обрушение)
- МРР: остальные сборники (пилот 4.2 уже в БД); поле region у книг для
  авто-выбора Москва/МО; экстраполяция МРР ф.2.2 (+в×ΔX×0,5) в _match_row

## Лог

### 2026-05-08
- Составлен план, утверждена архитектура
- Начало реализации: Docker, DB schema, auth, frontend skeleton, entity extractor

### 2026-07-22
- Бенчмарк на ТЗ «НС7 ячейки+ПЧ» против эталонной сметы (Александров, Инфострой)
- Движок: штучные строки (b=NULL, без диапазона → a×X); per-book ПД/РД
  (`reference_books.pd_pct/rd_pct`, миграция e5f6a7b8c9d0); `book_conditions.apply_mode`
  (миграция f6a7b8c9d0e1) вместо белых списков ключей; конверсии км/м↔п.м
- Warnings в результате расчёта: дроп коэффициента без условия, устаревший/отсутствующий индекс
- temperature=0 на всех AI-вызовах; Pass 3 получает единицы таблиц справочника
- КП: строки ПД/РД суммируются из позиций (не пересплит 40/60); fix фильтра ИГИ на geology page
- Сид 82 условий НЗ-847 (`seed_sito_conditions.py`): Разд.2 + все табл. 3.X.1
  (vision-извлечение из PDF приказа); опасные «за каждую последующую» — с coeff_key=NULL
- Восстановлена БД из ib_calculator.dump (dump не коммитить — перс. данные)

### 2026-07-22 (вторая сессия — НЗ-53-ВК + 707/пр)
- ВАЖНО: табл.2.3 НЗ-847 и табл.1.2 НЗ-53 (П=60/Р=40) — ТОЛЬКО для информационной
  модели (BIM); обычное распределение = 40/60. pd_pct/rd_pct СИТО откачен на NULL
- Движок: 707/пр п.131 ф.8.4/8.5 (X<Xмин/2 → Кэ=X/(0.5·Xмин), ≥0.1) и п.133
  ф.8.6-8.8 (a-only таблицы: интерполяция/сглаженная 0.6 экстраполяция)
- НЗ-53-ВК реимпортирован (195 строк, тыс.руб как есть — БЫЛ двойной масштаб ×1000
  и битые запятые в «б»); import-скрипт исправлен; spot-checks Кашина сходятся
- Сид 90 условий НЗ-53 (`seed_ms53_conditions.py`): табл.2 общих положений,
  все 3.X.1, реконструкция К≤1.5 / капремонт К≤0.5 по 707/пр пп.156-159 (НЗ-53 пп.17-18)
- `audit_book_rows.py` — универсальный аудит строк книги (непрерывность цен на границах
  диапазонов + магнитуда b). НЗ-53 чист; у СБЦП-17 28 подозрений (в т.ч. x_unit «км»
  вместо «м» в табл.8) — НЕ разобрано, техдолг
- Единицы: м³↔тыс.м³, длинные формы НЗ-53, м²/га в _PHYSICAL_UNITS
- Hints (`seed_asutp_rza_hints.py`): калибровка факторов АСУТП (Ф8 консервативно,
  Ф5/Ф9/Ф10 по перечню ТЗ, Ф7 группа+ПЧ→п.4.3) + РЗА «применительно» СИТО→СБЦП-13
  табл.11; восстановлен description п.12 табл.11 СБЦП-13. Бенчмарк НС7: +26% → +6%
- АСУТП: невалидные коды факторов (Ф10=п.2.3) теперь дают warning, не тихий 0 баллов
- `documents/minstroy/` реструктурирован: publish/ (в БД: СИТО, НЗ-53, ИГИ,
  ХимВолокна, МУ-620, 707пр, письмо 20212-ИФ) и unpublish/ (геофиз 282пр,
  геодез 812пр, письма без индексов в БД); имена файлов: «КОД - Описание (Приказ N).pdf»
- Бенчмарк №2 «Барвиха Инвест» (ВО ИЖС, эталон по МРР+индекс МКЭ 9,923):
  коллектор 1,5 км по НЗ-53 ф.8.4 = 877 тыс vs 1 100 тыс МРР (80%). Выводы:
  (1) sbts_object_type_id критичен — без него exact-match чужой строки (дюкер)
  бьёт экстраполяцию правильной; (2) для Москвы/МО нужна база МРР (roadmap);
  (3) камеры/колодцы в федеральных НЗ включены в цену коллектора («с сооружениями
  на нем») — не создавать отдельные позиции, в отличие от МРР
- Книги изысканий: `calc_method='survey'` (ИГИ/ИГДИ/ИГФИ) — основной calculate()
  их НЕ считает (строки в РУБЛЯХ, не тыс.руб!), выдаёт ошибку «блок Изыскания».
  Импорт survey-книг: `backend/scripts/import_nz_survey_book.py` (рубли as-is)
- Таблица техотчёта изысканий — data-driven через book_conditions:
  coeff_key='report_table' (номер таблицы), 'report_cat_N' (row_range категории).
  Хардкод табл.65 из igi_calculator удалён

### 2026-07-22 (третья сессия — бенчмарк №3 «Кашин ОС»)
- Эталон: смета Инфострой на КОС льнокомбината 2000 м³/сут (ИГИ 3 285 + ПД 8 223
  + экспертиза 266 + РД 13 309 = 25 083 тыс без НДС; книги ХимВолокна/НЗ-53/СБЦП-17,
  ИГИ по СБЦ-1999 × 80,58)
- ФИКС extract_entities_openrouter: модель пишет рассуждение до tool_call, обрезка
  по max_tokens → OpenRouter отдаёт arguments="{}" (finish_reason=length). Теперь
  `_call` ретраит 1 раз с ×4 бюджетом; битые аргументы pass 1 → понятная ошибка
  в missing_data вместо pydantic-traceback. До фикса ЛЮБАЯ экстракция валилась.
  (uvicorn --reload не видит правки на macOS bind mount — нужен docker restart)
- Прогон (sonnet-4.6): проектные без АСУТП 22,3 млн vs эталон ПД+РД 21,5 млн (+3,6%).
  Главная позиция: КОС по НЗ-53 т.12 X=2 = 14,4 млн ↔ эталон ЛОС ХимВолокна т.4-7
  + КЖ СБЦП-17-10-041 = 14,7 млн (−2%) — разные книги, та же величина
- ТЗ без объёмов (все X в ПД по внешним ссылкам): экстрактор честно ставит
  x_value=null + 22 пункта missing_data. СДЕЛАНО: warning на каждый used_minimum
  («подставлен минимум таблицы, цена УСЛОВНАЯ» + x_value_missing_reason)
- АСУТП-выброс: СБЦП-22 при нулевых данных дал 20,8 млн «полной системы».
  СДЕЛАНО: warning в _calculate_asutp_position — «факторы назначены AI БЕЗ
  количественных данных ТЗ» (детектор: x_value=null + x_value_missing_reason);
  при наличии данных — мягкое «сверьте факторы с перечнем ТЗ»
- СДЕЛАНО: кап экстраполяции вверх в _match_row — X_расч ограничен 2×X_гран
  (МУ №620 Прил.1; эталон так считает ЛОС 2000 м³/сут: 0,4×200+0,6×400)
  + warning «проверьте выбор таблицы/индивидуальный расчёт». RowMatch.x_capped
- Техотчёт ИГИ: п.121/табл.65 НЗ-281 ВЕРИФИЦИРОВАН по скану (листы 88–90):
  X = «общая стоимость КАМЕРАЛЬНЫХ работ» (прим.2: программа не учитывается,
  лабораторные НЕ входят). СДЕЛАНО: base_mode 'kameral' (report_base=3
  в book_conditions, сид для НЗ-281; 1=лаб+камерал деф., 2=+полевые).
  Отчёт 1 447 → 452 тыс. Прим.3: линейные объекты с профилями — К21=1,1 (не реализовано)
- ИГИ-блок итог (объёмы эталона руками): 4,29 млн vs 3,28 млн (+31%) —
  остаток структурный: лаборатория НЗ-281 из отдельных определений дороже
  «комплексов» СБЦ-1999; это разные книги, не баг
- Программа ИГИ табл.66 НЕ авто: цена фикс. по площади×глубине (не от стоимости),
  механика program_table/_interpolate_cost_table не подходит — добавлять item руками
  (п.122 гл. X: программа = отдельная работа, в базу отчёта не входит)
- Не покрываем: сопровождение экспертизы (форма 3П, калькуляция трудодней ~1% сметы)
- ГЛАВНЫЙ функциональный пробел по Кашину: проценты разделов ПД/РД — эталон
  умножает каждую позицию на Σ% фактических разделов (86%, 94,5%, 65%, 18%…),
  мы всегда 100% → систематическое завышение частичных объёмов. Это roadmap
  `chapter_coefficients`; список разделов есть в ТЗ п.8 «Содержание работ»

### 2026-07-22 (четвёртая сессия — разделы + МРР-пилот)
- Свежий дамп БД снят (включая report_base=3 и всё ниже)
- Проценты разделов ПД/РД реализованы end-to-end (см. секцию book_section_shares
  выше). Сид НЗ-53: 387 записей из приложения к приказу (vision, листы 3-7).
  Тест на Кашине: усреднитель ПД 85% / РД 93% — копейка в копейку с эталоном;
  поле sections добавлено в ExtractedEntity и tool-схему экстрактора
- МРР-пилот: книга МРР-4.2.04-22 (id=16, base 2000, «Инженерные сети и
  сооружения») — таблицы 3.1 водопровод + 3.3 канализация/коллекторы/ГНБ
  (85 строк, текстовый слой PDF чистый), Кср прил.1 → section_shares,
  8 условий из примечаний, индекс 2000→9,923 (Кпер МКЭ, номер приказа уточнить).
  Кв МРР = 40/60 = дефолт МУ №620, pd_pct/rd_pct не нужны
- Смоук-тест: коллектор 1500 п.м табл.3.3 п.1 ПД = 1 089,5 тыс vs эталон
  Барвихи по МРР 1 100 тыс (−1%!). Барвиха-гэп «80% от МРР» закрыт региональной базой
- Заметка: экстраполяция вверх у МРР — своя ф.2.2 (a+в·Xmax+в·ΔX·0,5), НЕ МУ №620;
  в пилоте не поддержана (внутри диапазонов не влияет) — техдолг с полем у книги

### 2026-07-22 (пятая сессия — прогон Барвихи, OCR на OpenRouter)
- `document_parser._parse_pdf_vision` переведён на OpenRouter (image_url + data-URI,
  модель `settings.ocr_model`, деф. anthropic/claude-sonnet-4.6) — сканы снова читаются
- Кэш OCR: `/stream` теперь пишет распарсенный текст в `project_files.extracted_text`
  и переиспользует его — повторный прогон экстракции не жжёт vision-токены
- Бенчмарк №2 Барвиха ЗАКРЫТ по РД: сеть 1500 п.м по МРР табл.3.1 п.2 × Ксл 0,9
  = 1 099,548 тыс; камеры 4 шт = 374,494; узел врезки = 63,110 — ВСЕ копейка
  в копейку с эталоном (итог РД 1 537,2 = эталон без благоустройства).
  Благоустройство 440,6 тыс — МРР-7.1 (не импортирован, очередь)
- МРР-4.2 реимпорт (id=17): типы объектов PER ПУНКТ таблицы (пересекающиеся
  диапазоны X пунктов 3.1-2/3/4/5 ломали слепой матч); + Ксл I=0,9/III=1,2
  (табл.2.2), реконструкция 1,2 (табл.2.1), капремонт 0,8, уникальные 1,2,
  охранный ландшафт 1,2 — все book-wide условия
- Экстракция Барвихи (sonnet-4.6): stage='Р' угадан; изыскания из «предоставляется
  исполнителем» пойманы (ИГДИ т.46/47, ИГИ т.65/66); НО создала ДУБЛЬ сети
  (СИТО т.305 + МРР т.33) — нужен регион-роутинг книг (МО→МРР, поле region);
  длины сетей нет ни в ТЗ, ни в договоре (эксперт брал 1500 п.м из генплана)
- Изыскания Барвихи эксперт считает по МРР-3.1 (геодезия) с МКЭ 9,923 —
  федеральные НЗ-812/281 дадут другую структуру; МРР-3.1/3.2 в очередь
- Lookup коэффициентов: AI кладёт coeff_key в поле `name` CoefficientInput —
  конвенция подтверждена (resolve по name)

### 2026-07-22 (шестая сессия — оцифровка МРР глав 3 и 7 без AI)
- `mrr_digitize.py` — generic-парсер таблиц МРР из текстового слоя PDF
  (pdfplumber, БЕЗ AI): «Таблица N» + продолжения, шапки любой глубины,
  двойные ячейки полевые/камеральные, режим ab_dense для «плавающих» сеток
  а/в, нормативы-формулы сохраняются текстом. Результаты —
  `scripts/mrr_digitized/*.json` (реимпорт без переоцифровки)
- `import_mrr_digitized.py` — 15 книг: гл.3 (изыскания, survey, РУБЛИ) +
  гл.7 (благоустройство, standard, тыс.руб). ~4300 строк, ~280 таблиц.
  table_num = id без точек («3.1.4»→314, «4.2.3.1»→4231). Типы per пункт.
  Ценовые колонки: survey — медиана>25 руб; standard — «цена» в подписи
- Спот-чеки: ВСЕ эталонные значения Барвихи найдены (3.1.4 п.3 5690,4/2644,08;
  3.2.3 п.5 2908,07/1111,6; бурение 386,66; монолиты 230,59; коррозионка
  300,89/138,6; программа 5923,13; 7.1 табл.2.2.1 п.13 а=8 в=44)
- Благоустройство 1,5 га по МРР-7.1 = 440,581 тыс — копейка в копейку.
  РД-блок Барвихи ПОЛНОСТЬЮ: 1977,8 ≈ эталон 1977,7 тыс
- PDF глав 3 и 7 перенесены в documents/minstroy/publish/mrr/
- Survey-индекс МКЭ база 2000 = 9,923 добавлен (work_type='survey')
- Расчётные пробелы survey-МРР (техдолг): транспорт % (табл.2.2), организация/
  ликвидация % (табл.2.3), отчёт геодезии «фикс+%» (табл.4.11.1/2 — сохранены
  текстом в conditions), отчёт геологии 32% от камеральных — движок ИГИ
  этих процентных позиций пока не строит (в эталоне они отдельные строки)
- Пропуски парсера — только классификаторы категорий сложности (без цен),
  ~25 таблиц на 15 книг; список в выводе import-скрипта

### 2026-07-23 (седьмая сессия — верификация методик, golden-тесты, survey-МРР, регион)
- ВЕРИФИЦИРОВАНО по первоисточникам (кап экстраполяции был применён слишком широко):
  · МУ №620 п.2.1.3: экстраполяция в пределах [Xмин/2; 2×Xмакс]; вне — ф.3П
    (п.2.1.4). Движок: кап X_расч + warning «требуется калькуляция 3П»
  · 707/пр п.131-2 ф.8.3: вверх БЕЗ ограничения (капа у НЗ-книг НЕТ)
  · МРР ф.2.2: вверх a+в·Xмакс+в·ΔX·0,5
  → `reference_books.pricing_method` ('mu620'|'707pr'|'mrr', миграция
  b8c9d0e1f2a3), ветвление в _match_row. Для mu620 ниже Xмин/2 тоже кап+3П
  (ф.8.4/Кэ — только 707pr)
- МЕХАНИЗМ ЛОКАЛЬНОГО ТЕСТА: `backend/tests/test_golden.py` — 11 golden-тестов
  БЕЗ AI/токенов (~0,3 сек): docker exec backend pytest tests/test_golden.py -q.
  Каждое ожидание — из эталонной сметы или формулы методики с источником
  в докстринге. Прогонять после ЛЮБОЙ правки движка/сидов
- Survey-МРР процентные механики: IgiItem.k (множитель примечаний таблиц),
  work_category='percent' (pct, percent_base: field|lab|kameral|field+percent,
  counts_as — зачисление в категорию, напр. камеральная обработка 20% от лаб
  → в базу техотчёта). Изыскания Барвихи собираются: ЛС-01 геодезия 593 641,7
  копейка в копейку; ЛС-02 геология 540 756 ПО ДОКУМЕНТУ (табл.9.3.1: отчёт
  32% от камеральных БЕЗ повторного индекса; в эталоне индекс задвоен —
  эталонные 787 705 содержат 274 624 отчёта вместо 27 675, расхождение
  задокументировано в тесте)
- Регион-роутинг: `reference_books.region` (миграция c9d0e1f2a3b4, МРР →
  «Москва и МО»), правило в _build_book_list: региональные только для своего
  региона, приоритетнее федеральных, дубли запрещены
- Молчаливый fallback «тип без строк в таблице → матч без типа» теперь даёт
  warning (RowMatch.type_fallback) — ловил чужую строку (узел врезки → камера)
- Кашин после разделов: проектные без АСУТП 20,39 млн vs эталон 21,53 (−5,3%;
  было +3,6% без разделов — состав позиций у нас и эталона разный)

### ДООЦИФРОВАТЬ (очередь оцифровки)
- МРР гл.4 остальное: 4.1 ОКС (здания!), 4.3, 4.4, 4.8 (проектные от стоимости
  строительства), 4.9 (газ), 4.10-4.16; гл.5 (слаботочка/АСУ), гл.2, 6, 8-13
- МРР гл.1 (общие указания 1.1) — правила применения, Кпер: официальный
  номер письма МКЭ для 9,923 (сейчас из эталона: МКЭ-ОД/26-10 от 20.02.2026)
- Формульные нормативы отчётов геодезии МРР-3.1 табл.4.11.1/4.11.2
  («фикс+% от превышения») — лежат текстом в conditions, механики нет
- Классификаторы категорий сложности изысканий (пропущены парсером осознанно) —
  нужны AI для выбора категории I/II/III; оцифровать как hint-тексты
- СБЦП-17 табл.8 единицы (км vs м) — старый техдолг, аудит 28 подозрений
- НЗ-53: распределения разделов для пропущенных row_range-вариантов
  (камера переключения, дюкер, метантенки, воздуходувная, павильон/аппаратная)

### 2026-07-23 (восьмая сессия — ПОЛНАЯ оцифровка МРР, примечания как условия)
- Оцифрованы ВСЕ оставшиеся главы МРР (1, 2, 4, 5, 6, 8, 9, 10, 11+12, 13):
  70 сборников, 797 таблиц — конвейером mrr_digitize.py, 0 фейлов.
  Итого в БД 86 МРР-книг, 24 224 строки, JSON в scripts/mrr_digitized/
  + manifest.json (код/имя/метод из имён файлов; 4.2 исключён — кураторский)
- ПРИМЕЧАНИЯ К ТАБЛИЦАМ теперь оцифровываются: collect_notes() собирает блоки
  «Примечание(я):» после таблиц, режет на пункты, извлекает коэффициенты
  («с коэффициентом N,N», «К=N,N») → book_conditions (coeff_min при
  единственном коэффициенте, effect_type=flag при тексте без коэфф.).
  1 156 условий из примечаний по всем МРР-книгам
- Все PDF МРР перенесены в publish/mrr (unpublish/mrr пуст)
- Регион-правило уточнено: «правило выбирает справочник, а не отменяет вид
  работ» — фикс регрессии v2-прогона Барвихи (пропали изыскания)
- Golden: 11/11 после массового реимпорта
- Известное: типы/строки новых 70 книг — generic-нарезка (описания местами
  шумные, «I | II | III :: …»); кураторская полировка по мере использования
  книг в бенчмарках (образец — import_mrr_4_2.py)

### 2026-07-23 (девятая сессия — разбор всех НЗ/СБЦП из unpublish)
- `sbc_digitize.py`: два новых формата БЕЗ AI — псевдографика │─┬ (СЦ-87/
  комплексдок-конвертации; колонки а/б + стадийные К, диапазоны «От/Св./"»)
  и whitespace-колонки (хвостовые числа = а, б[, %ПД, %РД]). Выход совместим
  с mrr-конвейером; примечания с коэффициентами собираются
- Разобрано 88 файлов unpublish/sbc: итог в БД 181 книга, 42 996 строк,
  1 804 условия. Активных 95: кураторские + МРР + НЗ. НОВЫЕ книги из sbc —
  ВСЕ НЕАКТИВНЫ намеренно: СЦ-91 (~2 800 точных строк псевдоформата) — нет
  индексов к базе 1991; ws-черновики (62 шт, вкл. 11 совр. СБЦП-2001) —
  разбор грубый, требует выверки. Кураторские книги защищены от затирания
  (проверка notes NOT LIKE 'Оцифровано%'), сработала дважды
- Аудит: negative a/b = 0; x_min>x_max = 6 (все в неактивных черновиках);
  2 активные пустышки деактивированы (МРР-3.2.81-12, СБЦП 81-2001-07)
- PDF перенесены в publish/sbc/auto. Экспресс-аудит SQL вместо
  audit_book_rows.py (скрипт не был закоммичен — воссоздать)

### НЕ ДООЦИФРОВАНО (осознанно, очередь)
- ws-черновики: 62 книги (вкл. СБЦП-2001-01/02/03/04/06/08/12/15/16/18 —
  дороги, ЖД, искусственные сооружения, метрополитены!) — ws-парсер снимает
  частично; нужна доработка формата (многострочные шапки, %ПД/РД колонки)
- Сканы: prikaz-30pr-pril1/2/3 (нет текстового слоя — vision)
- Приказы НЗ (unpublish/nz/prikazi: 281, 282, 53, 812, 847) — первоисточники
  УЖЕ импортированных книг; сверка оцифровки с приказами — отдельная задача
- Письма 2026q2 (4 шт) — индексы к базам, не используемым активными книгами
- СЦ-91: активация только после появления индексов к базе 1991

### ПЛАН ВЫВЕРКИ И ДООЦИФРОВКИ (утверждён 23.07.2026, двигаемся по шагам)

Решения: СЦ-91 — АРХИВ (не активировать, индексы не искать).

**Открытие прогонов №4-6**: эксперт считает современными федеральными НЗ
(2021-2024) и СБЦП-25, которых у нас НЕТ в файлах — они важнее выверки
ws-черновиков старых СБЦП. Точные коды из эталонных смет:
  · Котельная: НЗ-2024-МС53-ВК ✓есть, СИТО ✓, СБЦП-22 ✓, НЗ-2024-МС52-ГРС ✗,
    НЗ-2022-МС849 (котельные) ✗
  · Самолёт: СБЦП-2001-25 (внутр. инженерные системы зданий) ✗,
    НЗ-2021-МС828-ИТСО ✗, НЗ-2021-МС848-ОЖГС ✗, ИГДИ/ИГИ ✓, СИТО ✓, СБЦП-02 ч.

Шаги (в порядке выполнения):
1. СКАЧАНО пользователем 23.07 (лежит в unpublish, имена с пометками качества):
   a. СБЦП 81-2001-25 «Обмерные работы и обследования» (изд.2016, ЧИСТЫЙ текст,
      но МАТРИЧНЫЕ таблицы: этажность×категория здания×категория сложности,
      руб/100 м³) — ГЛАВНАЯ книга Самолёта (15 позиций ЛС-05: 787,4=II кат×
      2-я сложн; 659,8; фикс 2200/3300/2400; 1200/тыс.м²; преддоговорные 10% —
      percent-механика уже есть). НУЖЕН режим «matrix» в sbc_digitize +
      выверка обратным счётом ЛС-05 (670,3 тыс)
   b. НЗ-849 (газовая промышленность, ВКЛЮЧАЕТ котельные) и НЗ-848-ОЖГС —
      сканы с ТРАНСЛИТ-OCR (текстовый слой мусорный) → только vision
      постранично (50 и 79 стр)
   c. НЗ-52-ГРС и НЗ-828-ИТСО — кириллический OCR с ошибками в словах
      (цифры проверить!) → vision-сверка таблиц или осторожный конвейер
      со спот-чеком каждой таблицы
2. ws-парсер v2 для приоритетных имеющихся книг:
   a. «Обмерные работы и обследования зданий» (sbts-na-obmernye…) —
      нужен ЛС-05 Самолёта (670 тыс)
   b. СБЦП 81-2001-16 «Искусственные сооружения» (190 строк черновик)
   c. Проверить авто-коды СБЦП по титулам (регекс мог соврать) — сверить
      каждый активируемый код с титульной страницей
   Доработки: многострочные шапки, %ПД/РД колонки, «до/св.» диапазоны,
   двухколоночная вёрстка. Выверка: спот-чек 5 строк/книгу против PDF
3. Хинт/роутинг теплоэнергетики: после НЗ-849 — хинт «котельная целиком
   по НЗ-849; МРР-4.9 только газовая часть» (или регион-логика МО)
4. Калибровка АСУТП (сквозная переоценка во всех прогонах): факторы
   по умолчанию вниз при x_value_missing_reason; повторный бенчмарк НС7+СПб
5. Автосборка изысканий в survey-блок из entities (СПб: 4 позиции ушли
   в ошибки вместо блока изысканий)
6. Сверка приказов НЗ 281/282/53/812/847 с БД (выборочно, глазами)
7. Остальные ws-черновики — только по мере появления эталонов/запросов

### 2026-07-23 (десятая сессия — СБЦП-25: режим matrix, шаг 2a плана)
- `import_sbcp25.py` — кураторский импорт СБЦП 81-2001-25 (обмеры/обследования):
  МАТРИЧНЫЕ таблицы 1-4 по КООРДИНАТАМ слов (x-центры колонок высот; текстовые
  строки ломает OCR). Ячейка = отдельный object_type с говорящим именем
  («работы кат.2, здание кат.II, высота до 6 м») + штучная строка a×X
  (X = объём в сотнях м³) — матрица адресуется типом, не X-диапазоном
- Табл.15 (обследование систем) — выверенными данными (ступени объёма,
  тыс.руб; «зд»/«1Д» глюки разрешены по монотонности); добавки «на каждые
  последующие» — отдельные строки с пометкой ОПАСНО
- Условия: табл.10 (усложняющие 1,15-1,4) и табл.11 (объёмные 4,3..1,3) —
  КНИЖНЫЕ (table_num=NULL — урок: условия из «таблицы коэффициентов»
  привязываются не к её номеру, а ко всей книге); преддоговорные 10%
- 534 строки; встроенные assert'ы на эталонные ставки (787,4/659,8) ПОЙМАЛИ
  сдвиг колонок при первом прогоне (шапка высот расщепляется на 2 y-кластера,
  а строка нумерации колонок мимикрирует под неё)
- Golden: 12/12, новый test_samolet_ls05_sbcp25 — обратный счёт ЛС-05
  (обмеры 289 702 руб, обследования 242 755, ГВС 2200×1,2×7,1, электросети
  1200×1,127×7,1 — копейка в копейку). Потери OCR (2 слипшихся числа
  логируются импортом) — дочистить руками при случае
- Табл.12-14 (вибродинамика/бетон/пробы) — второй проход

### 2026-07-23 (одиннадцатая сессия — НЗ-849 и НЗ-52 vision, шаг 1b плана)
- НЗ-849 (газовая промышленность, скан-транслит) — прочитан ГЛАЗАМИ
  постранично (pdf2image→Read): import_nz849.py — табл.3.8 ПОЛНОСТЬЮ
  (объекты общетехнического назначения: ЦПУ/операторные, ХАЛ, подогреватели,
  РММ, ПЭБ, кабельные эстакады, электрообогрев, генплан; 23 строки) +
  условия табл.3.5.1/3.7.1 (13 коэфф.) + индекс база 2022 → 1,53.
  Код книги НЗ-2022-МС849 (база цен 2022). НЕ внесены табл.3.1-3.7/3.9+
  (листы 8-25) — дочитка по мере надобности
- НЗ-52-ГРС: import_nz52.py — табл.3.16-3.17 (теплогенераторные/котельные
  установки, 5 строк, база 2024). Остальное — дочитка
- Golden 13/13: test_kotelnaya_nz849_nz52 — ЦПУ 466 810,3 (ф.8.2 вниз ✓,
  К=0,7) и котельная установка 1 740 525,9 — копейка в копейку с эталоном
- К=0,7 объектов общетехнического назначения: источник в НЗ-849 не найден
  при частичном чтении — условие modular_07 с пометкой УТОЧНИТЬ
- Для полного пересчёта Котельной (эталон 18,7 млн) всё готово книжно:
  СИТО+НЗ-53 были, НЗ-849/НЗ-52 добавлены; хинт «котельная целиком по
  НЗ-52 т.3.17, не по МРР-4.9» — следующий шаг (регион МО выбирает МРР)
